---
title: "Projects"
layout: "base.njk"
---

<h1 class="mx-3 my-4 text-4xl font-bold text-gray-700 sm:text-5xl">Some of my projects</h1>

<div class="mt-5">
  {% for project in projects %}
    <div class="px-5 pt-0 pb-3 mb-5 bg-white shadow-lg rounded-xl">
      <h2 class="text-3xl font-bold text-gray-700 border-yellow-200 border-b-15 sm:text-4xl">
        <span class="relative inline-block ml-3 hover:scale-105 transform transition top-4">
        {{project.name}}
        </span>
      </h2>
      <div class='my-2'>
        <span class="font-bold text-gray-700">Technologies: </span>
      {% for tech in project.technologies%}
        <span class="inline-block px-3 py-1 m-1 font-bold text-gray-700 bg-yellow-100 rounded-lg shadow-sm">{{tech}}</span> 
      {% endfor %}
      </div>
      {% for feature in project.features %}
        <div class="px-3 mb-4 lg:flex {{ 'flex-row-reverse justify-between' if loop.index0 % 2 == 0 else 'space-x-5'}}">
          <img class="w-full p-2 bg-white border rounded-lg cursor-pointer lg:mx-0 lg:w-6/12 hover:scale-105 hover:shadow-lg shadow-sm transform transition" src="{{feature.photo}}" alt="">
          <div class="mt-4 ml-4">
            <h3 class='text-xl font-bold text-gray-600'>{{feature.title}}</h3>
            <p class='text-sm text-gray-600 sm:text-md'>{{feature.description}}</p>
            <ul class='ml-5 text-sm text-gray-700 list-disc list-outside'>
            {% for point in feature.points %}
              <li class=''>{{point}}</li>
            {% endfor %}
            </ul>
          </div>
        </div>
      {% endfor %}
      <div class='flex justify-center space-x-3'>
        <a
          class="inline-block mt-2 py-2 px-4 rounded-lg transform transition bg-blue-500 hover:bg-blue-400 hover:-translate-y-0.5 focus:ring-blue-200 focus:ring-opacity-50 focus:outline-none focus:ring tracking-wider font-semibold text-sm text-white shadow sm:text-base cursor-pointer"
          href="{{project.website}}"
          target="_blank"
          >Site</a>
        <a
          class="inline-block mt-2 py-2 px-4 rounded-lg transform transition bg-blue-500 hover:bg-blue-400 hover:-translate-y-0.5 focus:ring-blue-200 focus:ring-opacity-50 focus:outline-none focus:ring tracking-wider font-semibold text-sm text-white shadow sm:text-base cursor-pointer"
          href="{{project.github}}"
          target="_blank"
          >Github</a>
      </div>
    </div>
  {% endfor %}
</div>
